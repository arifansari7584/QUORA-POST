<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all post</title>
    <link rel="stylesheet"href="/style.css"/>
</head>
<body>
    <h1>quora post</h1>
    <% for(post of posts){%>
   <div class="post">
    <h3 class="user">@<%= post.username %></h3>
    <h4 class="content"><%= post.content%></h4>
    <a href="http://localhost:8080/posts/<%= post.id %>">see in detailed</a>
    <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
    <form method="post"action="/posts/<%=post.id%>?-method=delete">
        <button class="b">delete post</button>
    </form>
   </div>
   <% }%>
   <br>
   <br>
   <a href="http://localhost:8080/posts/new">create new post</a>
</body>
</html>